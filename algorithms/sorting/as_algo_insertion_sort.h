#ifndef ALGOSTRUCT_ALGO_SORTING_INSERTION
#define ALGOSTRUCT_ALGO_SORTING_INSERTION

#include <stdlib.h>
#include <stdio.h>
/*
 * pointer to array & size of array, sort order
 */
void insertion_sort(unsigned int*, int*, int);

#endif
