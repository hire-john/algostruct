#ifndef ALGOSTRUCT_ALGO_SORTING_SELECTION
#define ALGOSTRUCT_ALGO_SORTING_SELECTION

#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <unistd.h>

/*
 * pointer to array & size of array, sort order
 */
void selection_sort(int*, int*, int);

#endif